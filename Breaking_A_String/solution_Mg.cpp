#include <vector>
#include <iostream>
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;

    vector<int> split_arr(m);
    for (int i=0; i<m; i++){
        cin >> split_arr[i];
    }

    

    return 0;
}